export type Profile = {
  autoseen: boolean;
  nopinlist: boolean;
  numentry: number;
  numsubstr: number;
};

export type PinList = {
  feed_id: number;
  serial: number;
  title: string;
  update_at: string;
  url: string;
};
